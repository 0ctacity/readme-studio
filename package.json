{
  "name": "readme-studio",
  "version": "0.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "vite build",
    "test": "bun test",
    "typecheck": "tsc --noEmit && tsc --noEmit -p worker/tsconfig.json && tsc --noEmit -p worker/tsconfig.test.json",
    "serve": "vite preview",
    "worker:dev": "wrangler dev",
    "worker:types": "wrangler types",
    "worker:check": "wrangler types --check && tsc --noEmit -p worker/tsconfig.json && tsc --noEmit -p worker/tsconfig.test.json",
    "worker:deploy": "wrangler deploy"
  },
  "license": "MIT",
  "devDependencies": {
    "@solidjs/vite-plugin": "^3.0.0-next.29",
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.2",
    "vite": "^8.1.5",
    "wrangler": "^4.124.0"
  },
  "dependencies": {
    "@solidjs/web": "^2.0.0-rc.0",
    "dompurify": "^3.4.13",
    "hono": "^4.13.3",
    "jose": "^6.2.9",
    "marked": "^18.0.10",
    "solid-js": "^2.0.0-rc.0"
  }
}
